import { ResetPasswordRequestDto } from './reset-password-request.dto';

describe('ResetPasswordRequestDto', () => {
  it('should be defined', () => {
    expect(new ResetPasswordRequestDto()).toBeDefined();
  });
});
